// Copy this file to config.h and fill in your values
// config.h is gitignored

#define WIFI_SSID "your-wifi-ssid"
#define WIFI_PASSWORD "your-wifi-password"
#define API_ENDPOINT "https://smartgarden.duckdns.org"  // no trailing slash
#define SLEEP_SECONDS 3600                     // 1 hour between readings
#define RAW_DRY 2694                           // raw ADC in air (calibrate per board)
#define RAW_WET 1360                           // raw ADC in water (calibrate per board)
